[project]
name = "airport-checkin"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.15.0",
    "playwright>=1.59.0",
    "requests>=2.33.1",
]
